# Temporary placeholder in secrets/kafka-credentials.yaml
apiVersion: v1
kind: Secret
metadata:
  name: kafka-credentials
  namespace: kafka
stringData:
  SASL_USERNAME: "REPLACE_ME"
  SASL_PASSWORD: "REPLACE_ME"
type: Opaque