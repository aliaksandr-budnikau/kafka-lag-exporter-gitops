apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kafka-dev

namePrefix: dev-

labels:
  - pairs:
      environment: dev

resources:
  - ../../base

images:
  - name: kafka-lag-exporter
    newTag: v1.2.0-dev

patches:
  - path: patch-replicas.yaml